# OpenCV with the contrib modules (ArUco). Do not also install opencv-python:
# the two packages overwrite each other's cv2 module.
opencv-contrib-python>=4.8
numpy
# OpenGL cube demo (pose_obj_object.py)
pygame
PyOpenGL
# Depth-aware AR (ar_depth.py): MiDaS depth model runner and model download
onnxruntime
requests
